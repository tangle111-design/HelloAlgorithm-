# 给定 n 个物品，第 i 个物品的重量为 wgt[i - 1]、价值为 val[i - 1]。
# 背包容量为 cap，每个物品至多选择一次，但允许选择部分物品。
# 部分选择时价值按重量比例计算，求在容量限制下可获得的最大总价值。


class Item:
    """物品（用于打包重量与价值）"""

    def __init__(self, w: int, v: int):
        self.w = w
        self.v = v


def fractional_knapsack(wgt: list[int], val: list[int], cap: int) -> float:
    """分数背包：贪心练习版"""
    # 步骤 1：将 wgt 与 val 组装为 Item 列表。
    # 步骤 2：按单位价值（v / w）从高到低排序。
    # 步骤 3：从高单位价值物品开始遍历：
    #   - 若当前物品可完整放入：累加完整价值，并减少剩余容量。
    #   - 若当前物品不能完整放入：按比例累加部分价值后结束循环。
    # 步骤 4：返回最大总价值。
    """在此处完成代码"""
