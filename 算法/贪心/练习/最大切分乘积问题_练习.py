# 给定正整数 n，将其切分为至少两个正整数之和，
# 使这些整数的乘积最大，返回最大乘积。


import math


def max_product_cutting(n: int) -> int:
    """最大切分乘积：贪心练习版"""
    # 步骤 1：处理小规模边界（n <= 3 时必须切分出一个 1）。
    # 步骤 2：尽量切分为 3，计算：
    #   - a: 可切出的 3 的个数
    #   - b: 余数
    # 步骤 3：按余数分类处理（这是本题最关键的分支）：
    #   - b == 0: 直接返回 3^a。
    #   - b == 1: 将一个 3 + 1 调整为 2 + 2，返回 3^(a-1) * 4。
    #   - b == 2: 返回 3^a * 2。
    # 步骤 4：返回最大乘积。
    """在此处完成代码"""
