# 输入数组 ht，ht[i] 表示第 i 块竖板高度。
# 任取两块竖板 i, j（i < j）可形成容器：
# 容量 = min(ht[i], ht[j]) * (j - i)。
# 目标是找到最大容量。


def max_capacity(ht: list[int]) -> int:
    """最大容量：双指针贪心练习版"""
    # 步骤 1：初始化双指针 i, j 分别指向数组两端。
    # 步骤 2：在 i < j 条件下循环：
    #   - 计算当前容量 cap，并更新全局最大值。
    #   - 比较 ht[i] 与 ht[j]，向内移动较短板对应的指针。
    # 步骤 3：双指针相遇后，返回最大容量。
    """在此处完成代码"""
