def coin_change_greedy(coins: list[int], amt: int) -> int:
    """零钱兑换：贪心练习版（假设 coins 已按升序排列）"""
    # 步骤 1：从最大面值硬币开始尝试（索引指向 coins 末尾）。
    # 步骤 2：当剩余金额 amt > 0 时循环：
    #   - 向左移动索引，直到找到 <= amt 的硬币。
    #   - 选择该硬币，更新 amt 与硬币计数。
    # 步骤 3：循环结束后：
    #   - 若 amt 恰好为 0，返回硬币总数。
    #   - 否则返回 -1（无法凑出目标金额）。
    """在此处完成代码"""
